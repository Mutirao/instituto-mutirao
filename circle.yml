machine:

  timezone:
    America/Sao_Paulo

  ruby:
    version:
      2.1.1

dependencies:
  post:
    - bundle exec jekyll build:
        timeout: 300
test:
  override:
    - echo "No tests are needed"

deployment:
  gh-pages:
    branch: gh-pages
    commands:
      - bash deploy.sh
